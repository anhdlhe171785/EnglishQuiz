@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using PRN222_EnglishQuiz.Models

@{
    ViewData["Title"] = "Exam History";
    var history = ViewBag.ExamHistory as List<ExamHistory>;
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h2 class="fw-bold text-primary">📚 Lịch sử làm bài thi</h2>
        <p class="text-muted">Xem lại những bài thi bạn đã hoàn thành</p>
    </div>

    @if (history != null && history.Any())
    {
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            @foreach (var e in history)
            {
                <div class="col">
                    <div class="card border-0 shadow rounded-4 h-100">
                        <div class="card-body">
                            <h5 class="card-title text-success fw-bold">
                                <i class="bi bi-journal-text me-2"></i> @e.Exam?.Title
                            </h5>
                            <p class="text-muted">@e.Exam?.Description</p>

                            <ul class="list-unstyled mt-3 mb-0">
                                <li><strong>Điểm:</strong> <span class="badge bg-info text-dark">@e.Score</span></li>
                                <li><strong>Ngày thi:</strong> <span class="text-secondary">@e.DateTaken?.ToString("dd/MM/yyyy")</span></li>
                            </ul>
                        </div>
                        <div class="card-footer bg-transparent border-0 d-flex justify-content-between align-items-center">
                            <i class="bi bi-clock-history text-secondary"></i>

                            <form asp-controller="Exam" asp-action="Detail" method="post">
                                <input type="hidden" name="id" value="@e.Id" />
                                <button type="submit" class="btn btn-sm btn-outline-primary">
                                    Xem chi tiết
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-warning text-center mt-5">
            <i class="bi bi-info-circle-fill me-2"></i> Bạn chưa có lịch sử làm bài nào.
        </div>
    }
</div>
